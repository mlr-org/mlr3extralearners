# Check if an example is runnable

Checks if an example is runnable by loading the required packages.

## Usage

``` r
learner_is_runnable(id)
```

## Arguments

- id:

  (`character(1)`)  
  The id of the learner.
